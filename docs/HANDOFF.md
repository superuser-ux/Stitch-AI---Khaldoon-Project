# HANDOFF (pointer)

The canonical agent-handover / resume brief for this repository is the repo-root
**[`../HANDOFF.md`](../HANDOFF.md)** — read that first, then **[`../BUILD_STATE.md`](../BUILD_STATE.md)**
(the milestone tracker).

This file exists only so the `docs/HANDOFF.md` path resolves to the real brief; it is intentionally
not a second copy, to keep a single source of truth.
